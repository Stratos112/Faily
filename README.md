# Faily
Framework for local Neaural Net TTS, Foley and sFX. Useful for sound design/game development/digital media production.

use 
```set FAILY_NATIVE=1```
to bypass the browser

use 
```for /f "tokens=5" %a in ('netstat -aon ^| findstr :7842') do taskkill /F /PID %a```
to kill old runs

All Rights Reserved, Sky Vercauteren June 2026
